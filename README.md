# mingw-package-updater

Script to update MINGW packages.

## Use

`update-mingw.sh mingw-w64-python*`

## Dependencies

python: lastversion

```
python -m venv .venv
source .venv/bin/activate
pip install lastversion
```
